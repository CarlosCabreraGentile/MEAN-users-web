import { JustNumberDirective } from './just-number.directive';

describe('JustNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new JustNumberDirective();
    expect(directive).toBeTruthy();
  });
});
